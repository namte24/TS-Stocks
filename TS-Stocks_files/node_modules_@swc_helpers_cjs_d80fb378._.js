(globalThis.TURBOPACK || (globalThis.TURBOPACK = [])).push([typeof document === "object" ? document.currentScript : undefined,
"[project]/node_modules/@swc/helpers/cjs/_interop_require_default.cjs [app-client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

/**
 * Normalize a module import so it has a default-export shape.
 * @param {*} obj - The imported module value to normalize.
 * @returns {object} The original object if it signals an ES module (`__esModule` truthy), otherwise an object with a `default` property referencing the original value.
 */
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
exports._ = _interop_require_default;
}),
"[project]/node_modules/@swc/helpers/cjs/_interop_require_wildcard.cjs [app-client] (ecmascript)", ((__turbopack_context__, module, exports) => {
"use strict";

/**
 * Selects the appropriate WeakMap cache used for wildcard import interop.
 * @param {boolean} nodeInterop - If true, selects the Node interop cache; otherwise selects the Babel interop cache.
 * @returns {WeakMap| null} A WeakMap instance to use as the cache, or `null` if `WeakMap` is not available.
 */
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
/**
 * Normalize a module import into a wildcard-like object for interop between CommonJS and ES modules.
 * 
 * Copies enumerable own properties (preserving getters/setters when possible) from the input module
 * onto a new object, sets a `default` property referencing the original module, and caches the result.
 * If `obj` is already an ES module and `nodeInterop` is false, returns `obj` unchanged. If `obj` is
 * a primitive or null, returns an object with a single `default` property.
 *
 * @param {*} obj - The imported module value to normalize.
 * @param {boolean} nodeInterop - When true, prefer Node-style interop behavior; when false, prefer ES module shape.
 * @returns {object} An object containing the module's exported properties (excluding `"default"`) and a `default` property referencing the original `obj`, or `obj` itself when it already represents an ES module and `nodeInterop` is false.
 */
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) return obj;
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") return {
        default: obj
    };
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) return cache.get(obj);
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) Object.defineProperty(newObj, key, desc);
            else newObj[key] = obj[key];
        }
    }
    newObj.default = obj;
    if (cache) cache.set(obj, newObj);
    return newObj;
}
exports._ = _interop_require_wildcard;
}),
]);

//# sourceMappingURL=node_modules_%40swc_helpers_cjs_d80fb378._.js.map